# CrewAI A2A research agent — dependencies.
#
# IMPORTANT (2026-06-10): crewai and crewai-tools MUST be pinned together and
# kept in lockstep. The previous requirements pinned crewai==1.10.1 but left
# crewai-tools UNPINNED, so deploys installed a mismatched crewai-tools. The
# mismatch silently broke tool-calling: the agent would *narrate* a web search
# ("I will now proceed to search...") instead of actually invoking the Serper
# tool, hit max_iter, and return a refusal. Matched 1.14.x versions fix it.
# If you bump one, bump the other and re-test a web-search task.
crewai==1.14.6
crewai-tools==1.14.6

# Web server for the /a2a/task endpoint.
fastapi==0.136.3
uvicorn==0.49.0

# Pinned to the version validated against crewai 1.14.6.
openai==2.41.0
